#!/usr/bin/env php
<?php

declare(strict_types=1);

use Chetkov\HttpClientMitmproxy\Console\Command\MitmProxyCommand;
use Chetkov\HttpClientMitmproxy\DefaultFactory;
use Symfony\Component\Console\Application;

require_once dirname(__DIR__) . '/bootstrap.php';

//todo: откуда взять конфиг???

//todo: очищать папку tmp и redis при ЛЮБОМ завершении процесса

$application = new Application();
$application->add(new MitmProxyCommand(new DefaultFactory([])));
$application->run();
